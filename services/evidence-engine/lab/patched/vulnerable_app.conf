server {
    listen 8080;
    server_name shop-api-patched;
    location /api/health {
        return 200 '{"status": "healthy", "version": "1.2.9"}';
    }
    # /api/debug disabled in patched version
}
